#include <stdio.h>

int main() {
    int arr[] = {7, 4, 3, 9, 1, 6};
    int n = sizeof(arr)/sizeof(arr[0]);
    int fifth = -1;

    if (n >= 5)
        fifth = arr[4];  // 0-indexed

    printf("5th element: %d\n", fifth);
    return 0;
}
